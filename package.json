{
    "name": "AppElectron",
    "version": "1.0.0",
    "description": "app de controle de estoque de uma loja pequena",
    "main": "src/main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "electron ."
    },
    "dependencies": {
        "electron": "^28.1.0",
        "init": "^0.1.2",
        "moment": "^2.30.1",
        "sqlite3": "^5.1.7-rc.0"
    }
}
