{
  "name": "appelectron",
  "version": "1.0.0",
  "description": "Aplicativo Electron para controle de estoque, vendas, clientes e pagamentos.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node scripts/check-js.js",
    "package:win": "electron-packager . ControleComercial --platform=win32 --arch=x64 --out=dist --overwrite --ignore=dist --ignore=backups",
    "seed:test": "node scripts/seed-test-data.js"
  },
  "dependencies": {
    "sqlite3": "^5.1.7-rc.0"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-packager": "^17.1.2"
  }
}
